---
description: Force the THINK-FIRST loop — plan, predict failures, verify before claiming done.
allowed-tools: Read, Glob, Grep, Bash, Edit, Write
---
# /think-first
Apply AGENTS.md Rule 0 to: $ARGUMENTS
Do NOT edit yet. First output:
GOAL / ASSUMPTIONS / STACK CONFIRMED (files read) / PLAN (exact files+edits) /
STATES (loading·empty·error·success · mobile+desktop) / LIKELY FAILURE MODES (2-3).
Then implement. Before "done": re-read your diff as the skeptic, run the build,
run qa/visible-content-checklist.md, and paste the result block. Verified render
+ green build = done. "It probably works" = not done. Don't ask — state assumptions.
